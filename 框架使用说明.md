# 框架使用说明 
               by  zhengjiafeng

# 一  日志和闲话

看了很多七七八八的开源，要么大多成为了ccs开发环境的乖宝，要么用上了keil？当然正常用vsc的也是有
所以决定自己先用ccs搭台（保留成为ccs乖宝的权利），然后用codex修改使兼容最常用的cmake和岳麓框架的构型，使其最接近熟悉并且简单易用的环境
但由于很明显因为芯片的特殊性是有一段路要走的
md，Ti就是纯狗屎

虽然用自搭的新框架可能有点冒险，但是成功后对开发效率的提升是巨大的。
并且gpt5.5（5.6sol来也）和codex还是值得信任的合作伙伴的。


6.17
目前，起码跑通了编译，
@todo 研究如何通行sysconfig，尽量避开ccs这个我看着不爽的IDE
@todo 写一下环境配置的说明，有关的路径问题

6.18  
移植跃鹿框架并做底层适配

补充：
1  逐飞库好像有点意思（考虑通用兼容性，如果有空）
2  其实xrobot也有更成熟且直接通用的框架，但是队友好像没去接触，以及自己现在用起来其实也不是很顺手，所以就先舍弃了，虽然xr确实牛逼。

#  二   环境配置说明

打开工程后先确认这些东西已经装好：TI MSPM0 SDK、CCS或SysConfig（强烈推荐使用SysConfig直接打开.syscfg文件，CCS看着就烦，在本工程中也有提供压缩包）、Arm GNU Toolchain、CMake、Ninja。要烧录的话再确认OpenOCD和CMSIS-DAP/DAPLink驱动。

## VS Code/CMake里怎么选

1. 打开工程根目录 `MSPM0G3507_BASIC`。
2. 如果CMake Tools让你选Kit，不要选MSVC、MinGW这类电脑本机编译器；本工程要用 `arm-none-eabi-gcc`。
3. 确认Arm GNU Toolchain的 `bin` 已经加入系统PATH，终端里能直接运行 `arm-none-eabi-gcc --version`。
4. 本工程默认工具链文件是 `cmake/arm-none-eabi-gcc.cmake`，正常不需要手动改。

## 路径在哪里改

不同电脑最容易不一样的是这四个路径，都在 `cmake/mspm0g3507.cmake` 文件开头：

- `MSPM0_SDK_DIR`：MSPM0 SDK根目录。
- `SYSCONFIG_CLI`：`sysconfig_cli.bat` 的位置。
- `OPENOCD_EXE`：`openocd.exe` 的位置。
- `OPENOCD_SCRIPTS_DIR`：OpenOCD的 `scripts` 目录。



## 改路径后的注意事项

- CMake会缓存路径；换电脑、移动工程、升级SDK后，最好删掉旧 `build` 目录或重新配置。
- `Required MSPM0 toolchain path not found` 基本就是SDK、SysConfig或OpenOCD路径没对。
- `Could not find arm-none-eabi-gcc` 基本就是Arm GNU Toolchain没进PATH。
- 编译通过但烧录失败，优先查OpenOCD路径、探针连接、驱动和板子供电。
- 不要手改 `build/syscfg` 里的生成文件；引脚和外设配置改 `MSPM0G3507_BASIC.syscfg`。


# 三  框架使用手册

    SysConfig的配置源文件是工程根目录下的MSPM0G3507_BASIC.syscfg。可以用CCS或独立版SysConfig的图形界面编辑它，保存后由CMake调用SysConfig CLI读取MSPM0G3507_BASIC.syscfg，自动生成build/syscfg下的ti_msp_dl_config.c/.h、链接脚本和编译选项，随后再参与工程编译。修改引脚、外设和时钟时只改MSPM0G3507_BASIC.syscfg，不要手改build/syscfg里的生成文件。
    
    本框架通过修改cmakelist.txt支持freertos和nortos两种模式，也请注意这里的freertos是原生的，没有cmiss的中间适配

#  四   除此之外的tips

## BSP适配说明

BSP保留跃鹿框架给module层使用的核心接口，例如 `GPIOInstance`、`PWMInstance`、`USARTInstance`、`SPIInstance`、`IICInstance`，以及 `GPIORegister()`、`PWMRegister()`、`USARTSend()`、`SPITransRecv()`。

BSP对module/app暴露的函数名、结构体名和字段写法保持跃鹿风格，例如 `.htim = &htim1`、`.channel = TIM_CHANNEL_1`、`.usart_handle = &huart1`、`.spi_handle = &hspi1` 和 `.handle = &hi2c1`。这些轻量句柄由 `bsp_device.h/.c` 定义，并映射到SysConfig生成的TI外设实例。

`bsp_device` 只是接口适配边界，不实现任何其他平台的HAL函数。各外设BSP内部仍直接使用TI DriverLib；外设配置以 `MSPM0G3507_BASIC.syscfg` 和生成的 `ti_msp_dl_config.h/.c` 为准。

当前 `GPIO/PWM/SPI/IIC/USART/ADC/Timer/Capture/Flash/log/tools/init` 均已进入CMake构建。UART已配置RX/TX DMA和自维护IRQ；SPI和IIC目前使用带超时与错误检查的轮询传输，其IT/DMA枚举仅作为后续扩展入口。

Flash地址范围按当前MSPM0G3507链接配置定义为 `0x00000000 ~ 0x00020000`。真正保存参数前应单独划分参数区，避免擦写程序正文。

## 当前工程迁移注意点

### FreeRTOS时钟和tick

当前工程使用 `config/freertos/FreeRTOSConfig.h`，不要直接修改 TI SDK 安装目录中的配置。当前配置为：

```c
#define configCPU_CLOCK_HZ    ( ( unsigned long ) 80000000 )
#define configTICK_RATE_HZ    1000
```

当前SysConfig生成的 `build/syscfg/ti_msp_dl_config.h` 里 `CPUCLK_FREQ` 也是 `80000000`，所以现在两边是对得上的，`vTaskDelay(pdMS_TO_TICKS(1))` 基本就是1ms。

后续如果在 `MSPM0G3507_BASIC.syscfg` 里改了SYSCTL时钟，务必重新看生成文件里的 `CPUCLK_FREQ`，并确认它和FreeRTOS的 `configCPU_CLOCK_HZ` 一致。否则 `vTaskDelay()`、软件定时器、任务调度节拍都会不准，表现可能是“任务周期怪怪的”，而不是编译报错。

现在CMake使用工程内的FreeRTOS配置目录：

```cmake
MSPM0_FREERTOS_CONFIG_DIR =
    ${CMAKE_SOURCE_DIR}/config/freertos
```

如果以后再次修改SYSCTL时钟，先核对生成文件里的 `CPUCLK_FREQ`，再同步修改 `config/freertos/FreeRTOSConfig.h`。不要直接改TI SDK安装目录中的 `FreeRTOSConfig.h`。

### FreeRTOS堆和任务栈

工程配置里 `configTOTAL_HEAP_SIZE` 只有3KB。`robot_task.h` 直接使用 `xTaskCreate()`，用户任务的TCB和栈会从FreeRTOS heap里分配。

这意味着后续打开更多任务时，可能出现“能编译、能进main、但任务没创建成功”的情况。尤其注意：

- `xTaskCreate(..., stackDepth, ...)` 的栈深度以 `StackType_t` 为单位，不是字节；在Cortex-M0+上通常1个单位是4字节。
- `robot_task.h` 里如果给任务写 `1024`，实际大约就是4KB栈，已经超过当前3KB heap。
- `app/freertos_hooks.c` 只给Idle Task和Timer Task提供静态内存，不代表用户任务也是静态创建。

后续比较稳的做法有两个：要么把 `configTOTAL_HEAP_SIZE` 调大，并检查 `xTaskCreate()` 的返回值；要么把常驻任务逐步改成 `xTaskCreateStatic()`。

### Tickless idle和低功耗

工程内配置里 `configUSE_TICKLESS_IDLE` 当前是0，并且main初始化后会调用 `DL_SYSCTL_disableSleepOnExit()`。工程自有代码中也不应主动调用 `__WFI()`、`__WFE()` 或进入STOP/STANDBY，因此当前框架默认始终运行在正常工作状态。

SysConfig仍可能提示部分外设在STOP/STANDBY模式下不保留寄存器内容，这只是静态能力提示，不表示程序实际进入了低功耗。以后如果要重新启用低功耗，应通过本地 `FreeRTOSConfig.h` 和统一的电源管理入口处理。

### DMA和中断接收

UART当前已经配置RX/TX DMA、触发源、NVIC中断和 `UART0_IRQHandler`，`USARTSend` 支持阻塞、IT和DMA发送。接收按注册实例的固定缓冲区长度运行，收到完整DMA缓冲区后调用module回调。

SPI和IIC仍是轮询实现。后续为它们增加DMA或中断传输时，需要：

- 在 `MSPM0G3507_BASIC.syscfg` 里给对应外设配置DMA通道和trigger。
- 重新生成后确认 `ti_msp_dl_config.h` 里的DMA宏名、IRQ名和外设实例名。
- 在BSP里补对应ISR、回调、busy状态处理和缓冲区生命周期。
- 用逻辑分析仪或串口工具实际验证收发时序。

### SysConfig生成文件不要手改

`build/syscfg/ti_msp_dl_config.h`、`build/syscfg/ti_msp_dl_config.c`、`device_linker.lds`、`device.opt` 都是生成物。改引脚、外设实例、波特率、PWM频率、ADC通道、DMA配置时，改 `MSPM0G3507_BASIC.syscfg`，然后重新构建。

代码里不要猜宏名，先看生成的 `ti_msp_dl_config.h`。比如当前工程实际生成的是 `UART_0`、`SPI_0`、`I2C_0`、`PWM_0~PWM_3`、`TIMER_0` 等名字。

### DWT和时间测量

MSPM0G3507使用Cortex-M0+，当前 `bsp/dwt` 保留跃鹿时间接口名称：短延时使用 `delay_cycles`，调度器启动后的时间线主要依赖FreeRTOS tick。

如果后续要做高精度运行时间统计、微秒级调度或控制环耗时测量，建议使用已配置的硬件Timer作为时间基准。

### printf和日志输出

`app/syscalls_stub.c` 目前只是让newlib裸机工程能链接，里面的 `_write()` 会假装写成功，但不会真的把字符发到UART或RTT。因此 `printf()` 现在不是可见串口输出。

如果要把日志接到串口或RTT，需要改 `_write()` 或 `bsp_log` 的底层输出，并同时确认SysConfig里的UART波特率、TX引脚和上位机串口参数。

# 五  待做事项

1. 使用跃鹿原版典型module做接口编译验证，包括蜂鸣器/PWM、遥控器与裁判系统/USART、BMI088/SPI/GPIO和TFminiPlus/IIC。
2. 在SysConfig中配置MCAN/CAN-FD，并按跃鹿BSP接口完成CAN适配，供电机和板间通信使用。
3. 完善USART不定长接收，评估空闲检测、环形缓冲或分包解析方案；当前固定长度DMA不能覆盖所有遥控器和裁判系统协议。
4. 接通可见日志输出，确认RTT或UART日志方案，并让 `_write()`、`printf()` 和 `bsp_log` 的行为保持明确。
5. 补充HardFault等异常诊断，并在硬件上验证FreeRTOS的heap余量、任务栈高水位、malloc失败和栈溢出钩子。
6. 拿到测试板后验证Flash参数区的擦除、写入、读取、掉电保存和越界保护。
7. 拿到测试板后复查CMSIS-DAP/DAPLink烧录：测试SWD频率、NRST连接、正常下载、断电重连和整片擦除恢复流程，确认“烧录慢、需要擦除才能重连”的真实原因。
8. 为GPIO、PWM、Timer、Capture、ADC、SPI、IIC、USART和CAN建立最小硬件冒烟测试清单，硬件未验证的功能不标记为完成。

# MSPM0G3507 引脚使用说明

最后更新：2026-07-23

本文记录当前工程的 SysConfig 引脚和外部硬件要求。引脚配置源文件是工程根目录的 `MSPM0G3507_BASIC.syscfg`，`build/syscfg/ti_msp_dl_config.c/.h` 为自动生成文件，不应手工修改。

## 1. 当前引脚分配

### 调试和板载资源

| 信号 | MCU 引脚 | 用途 |
| --- | --- | --- |
| SWCLK | PA20 | SWD 时钟 |
| SWDIO | PA19 | SWD 数据 |
| 板载 LED | PB22 | 保留输出 |
| 板载按键 | PB21 | 保留输入 |
| BSL | PA18 | 保留输入 |
| 特殊保留 | PA2、PA21、PA23 | 天猛星开发板特殊资源 |

PA20/PA19、PB21、PB22、PA18、PA2、PA21、PA23 不建议再分配给扩展板其他功能。PA10/PA11 已用于板载串口相关资源，扩展板使用时要避免双重驱动。

### 电机和执行器

| 信号 | MCU 引脚 | SysConfig 外设 | 参数 |
| --- | --- | --- | --- |
| PWMA | PA0 | `MOTOR_PWM` / TIMA0 CCP0 | 20 kHz，初始占空比 0 |
| PWMB | PA1 | `MOTOR_PWM` / TIMA0 CCP1 | 20 kHz，初始占空比 0 |
| AIN1、AIN2 | PA17、PA16 | `MOTOR_GPIO` | 电机 A 方向 |
| BIN1、BIN2 | PB4、PB1 | `MOTOR_GPIO` | 电机 B 方向 |
| SERVO_PWM1 | PA29 | `SERVO_PWM_1` / TIMG6 CCP0 | 50 Hz，初始占空比 0 |
| SERVO_PWM2 | PA26 | `SERVO_PWM_2` / TIMG7 CCP0 | 50 Hz，初始占空比 0 |
| BUZZER | PB13 | `BUZZER_GPIO` | GPIO 输出 |

两路舵机均为 1 MHz 计数时钟、20000 个计数周期。舵机使用独立 5--6 V 电源，电源负端与 MCU 共地；接口附近建议放置 470--1000 uF 和 100 nF 去耦。电机驱动器的 PWM、EN 或 STBY 上电应默认关闭，建议在硬件上预留下拉电阻。

### 编码器和按键

| 信号 | MCU 引脚 | 当前配置 |
| --- | --- | --- |
| 左编码器 A/B | PA12、PA13 | GPIO 输入，双边沿中断 |
| 右编码器 A/B | PB20、PB23 | GPIO 输入，双边沿中断 |
| KEY1、KEY2、KEY3 | PB16、PA14、PA15 | GPIO 输入，无内部上下拉 |

编码器 GPIO 中断、`GROUP1_IRQHandler()`、四状态正交解码、累计计数和周期增量速度均已接入 BSP。四个引脚为双边沿触发，每个完整A/B周期产生4个计数；RPM和车速仍需在底盘层结合采样周期、编码器线数、减速比和轮径换算。若编码器为开漏/开集输出，需 4.7--10 kΩ 上拉到 3.3 V；推挽输出可不加，但建议预留 DNP 电阻位。按键建议每路 10 kΩ 上拉到 3.3 V、按下接 GND，可选 100 nF 消抖电容。

### OLED、MPU 和串口

| 功能 | 信号 | MCU 引脚 | SysConfig 实例/参数 |
| --- | --- | --- | --- |
| OLED I2C | SCL、SDA | PA31、PA28 | `OLED_I2C`，内部上拉已开 |
| MPU I2C | SCL、SDA | PB2、PB3 | `MPU_I2C`，400 kHz |
| MPU 中断 | INT | PA25 | GPIO 输入 |
| UART1 | TX、RX | PA10、PA11 | 115200 |
| UART2 | TX、RX | PA8、PA9 | 9600 |
| UART3 | TX、RX | PB15、PA22 | 9600 |

I2C 仍应使用外部上拉：OLED 建议 4.7 kΩ，MPU 的 400 kHz 总线建议 2.2--4.7 kΩ，均上拉到 3.3 V；模块自带上拉时不要重复并联过多。MPU_INT 若设为开漏，需约 10 kΩ 上拉；推挽模式可不加。UART 外接设备要 TX/RX 交叉并共地；PA10/PA11 与板载 USB 串口共用时，避免外部设备同时驱动同一网络。

### 超声波

| 信号 | MCU 引脚 | 状态 |
| --- | --- | --- |
| TRIG | PB14 | GPIO 输出，已配置 |
| ECHO | PA7 | 尚未配置输入捕获或边沿计时 |

常见超声波模块的 ECHO 为 5 V，接入 PA7 前必须增加分压或电平转换；不能直接把 5 V 输出接到 MCU。ECHO 的定时器输入捕获配置需后续在 SysConfig 中完成。

### 灰度传感器

本工程按三位地址选择、单路读取的方式使用 8 路灰度模块：`AD0/AD1/AD2` 输出通道地址，`OUT` 读取被选通道。

| 信号 | MCU 引脚 | 方向 | 分配状态 |
| --- | --- | --- | --- |
| AD0 | PB18 | 输出 | 已显式锁定 |
| AD1 | PB17 | 输出 | 已显式锁定 |
| AD2 | PB19 | 输出 | 已显式锁定 |
| OUT | PB24 | 输入 | 已显式锁定 |

四个灰度信号都已使用明确的 `$assign`。模块资料标注 5 V 供电，必须确认 AD 输入门限及 OUT 输出电压；OUT 不能未经验证直接输出 5 V 到 MSPM0，必要时加分压或电平转换。

## 2. 外部硬件注意事项

- 所有外部模块必须与 MCU 共地，I2C 和按键的上拉电源统一为 3.3 V。
- 电机和舵机电源不要从 MCU 的 3.3 V 稳压器取电，电机驱动和舵机电源应分别按负载能力设计。
- 扩展板不要复用 SWD、板载串口、BSL 和上述特殊/板载引脚。
- PCB 丝印应与表中 MCU 引脚和网络名称一致，尤其是两路舵机 PWM 名称。

## 3. 尚未完成的软件适配

- BSP 已映射 `UART1/UART2/UART3`、`OLED_I2C`、`MPU_I2C` 和四路 PWM 句柄；UART2/3 当前仅提供阻塞发送，未配置 DMA 接收。
- 编码器软件解码已实现，仍需实机确认左右方向、计数倍率和高速下的无效跳变率。
- ECHO 输入捕获尚未配置。
- 灰度模块的通道扫描和电平验证尚未完成。

## 4. SysConfig 验证

```powershell
cmake --build build --target syscfg
cmake --build build
```

生成后应检查 `build/syscfg/ti_msp_dl_config.h` 的实例名、引脚宏、IRQ 宏和定时器时钟。SysConfig 生成成功不等于外部电平、波形和模块行为已经硬件验证。

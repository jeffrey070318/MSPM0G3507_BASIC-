# MSPM0G3507 引脚使用说明

最后更新：2026-07-23

本文记录当前工程的引脚占用、SysConfig 外设实例以及仍待处理的硬件差异。引脚和外设配置的唯一源文件是工程根目录下的 `MSPM0G3507_BASIC.syscfg`；`build/syscfg/ti_msp_dl_config.c/.h` 是生成文件，不应手工修改。

## 1. 当前已配置引脚

### 1.1 调试接口

| 信号 | MCU 引脚 | 用途 | 备注 |
| --- | --- | --- | --- |
| SWCLK | PA20 | SWD 时钟 | 调试期间保留 |
| SWDIO | PA19 | SWD 数据 | 调试期间保留 |

### 1.2 电机驱动

| 信号 | MCU 引脚 | 方向/外设 | SysConfig 实例 | 当前参数 |
| --- | --- | --- | --- | --- |
| PWMA | PA0 | TIMA0 CCP0 输出 | `MOTOR_PWM` | 20 kHz，初始占空比 0 |
| PWMB | PA1 | TIMA0 CCP1 输出 | `MOTOR_PWM` | 20 kHz，初始占空比 0 |
| AIN1 | PA17 | GPIO 输出 | `MOTOR_GPIO` | 电机 A 方向控制 |
| AIN2 | PA16 | GPIO 输出 | `MOTOR_GPIO` | 电机 A 方向控制 |
| BIN1 | PB4 | GPIO 输出 | `MOTOR_GPIO` | 电机 B 方向控制 |
| BIN2 | PB1 | GPIO 输出 | `MOTOR_GPIO` | 电机 B 方向控制 |

`MOTOR_PWM` 使用 80 MHz 定时器时钟和 4000 个周期计数。BSP 中 `htim1`、`htim2` 均映射到同一个 TIMA0，通过不同 CCP 通道区分两路电机 PWM。

### 1.3 编码器

| 信号 | MCU 引脚 | 当前配置 | 备注 |
| --- | --- | --- | --- |
| L_ENCA | PA12 | GPIO 输入，双边沿中断 | 左编码器 A 相 |
| L_ENCB | PA13 | GPIO 输入，双边沿中断 | 左编码器 B 相 |
| R_ENCA | PB22 | GPIO 输入，双边沿中断 | 右编码器 A 相；与天猛星板载 LED 共用 PB22 |
| R_ENCB | PB23 | GPIO 输入，双边沿中断 | 右编码器 B 相 |

SysConfig 已生成 GPIOA/GPIOB 的 Group 1 中断配置，但工程目前尚未实现 `GROUP1_IRQHandler()` 和 A/B 正交查表解码。`bsp/encoder` 仍是读取硬件定时器计数器的接口骨架，因此当前编码器不能作为已完成功能使用。

PB22 已连接天猛星板载 LED。高速编码器接入前应确认板载 LED 电路不会影响电平和边沿；条件允许时建议将 `R_ENCA` 移到没有板载负载的 GPIO。

### 1.4 OLED 与 MPU

| 信号 | MCU 引脚 | 方向/外设 | SysConfig 实例 | 备注 |
| --- | --- | --- | --- | --- |
| OLED_SCL | PA31 | I2C0 SCL | `I2C_0_OLED` | 当前启用内部上拉 |
| OLED_SDA | PA28 | I2C0 SDA | `I2C_0_OLED` | 当前启用内部上拉 |
| MPU_SCL | PB2 | I2C1 SCL | `I2C_1_MPU` | 确认外部上拉到 3.3 V |
| MPU_SDA | PB3 | I2C1 SDA | `I2C_1_MPU` | 确认外部上拉到 3.3 V |
| MPU_INT | PA25 | 当前被配置为 GPIO 输出 | `MPU_GPIO` | 使用前必须改为输入，并按需求启用中断 |

I2C 总线不能被外部模块上拉到 5 V。模块板自带上拉时，需要确认其上拉电源为 3.3 V。

### 1.5 舵机 PWM

| 当前软件名称 | MCU 引脚 | 定时器通道 | 当前参数 | 扩展板图纸名称 |
| --- | --- | --- | --- | --- |
| `SERVO_PWM_1` | PA30 | TIMG6 CCP1 | 50 Hz，初始占空比 0 | SERVO_PWM2 |
| `SERVO_PWM_2` | PB21 | TIMG8 CCP0 | 50 Hz，初始占空比 0 | SERVO_PWM1 |

两路 PWM 都使用 1 MHz 计数时钟和 20000 个周期计数。当前软件实例编号与扩展板图纸的 PWM1/PWM2 名称相反；物理引脚均可输出 PWM，后续应统一命名，避免应用层操作错舵机。

舵机应使用独立 5 V 电源供电并与 MCU 共地，不应从开发板 3.3 V 稳压器取电。

### 1.6 超声波、按键、LED 与蜂鸣器

| 信号 | MCU 引脚 | 当前配置 | 备注 |
| --- | --- | --- | --- |
| TRIG | PB14 | GPIO 输出 | 已配置 |
| ECHO | PA7 | 未配置 | PA7 可使用 TIMG7 CCP1 做输入捕获 |
| KEY1 | PB0 | GPIO 输入 | SysConfig 名称暂为 `PIN_0` |
| KEY2 | PA2 | GPIO 输入 | SysConfig 名称暂为 `PIN_1` |
| KEY3 | PB16 | GPIO 输入 | SysConfig 名称暂为 `PIN_2` |
| LED3 | PB25 | GPIO 输出 | 已配置 |
| LED4 | PB26 | GPIO 输出 | 已配置 |
| BUZZER | PB13 | GPIO 输出 | 已配置 |

若超声波模块的 ECHO 输出为 5 V，PA7 前必须增加分压或电平转换。按键当前未配置内部上拉/下拉，硬件和软件至少要提供一种确定的空闲电平。

### 1.7 UART

| 扩展板名称 | MCU 引脚 | 当前 SysConfig 名称 | 当前波特率 | 备注 |
| --- | --- | --- | --- | --- |
| UART1_TXD | PA10 | `UART_0` TX | 115200 | 天猛星特殊引脚/板载串口资源 |
| UART1_RXD | PA11 | `UART_0` RX | 115200 | 天猛星特殊引脚/板载串口资源 |
| UART2_TXD | PA8 | `UART_1` TX | 9600 | SysConfig 默认值，使用前确认协议 |
| UART2_RXD | PA9 | `UART_1` RX | 9600 | SysConfig 默认值，使用前确认协议 |
| UART3_TXD | PA21 | `UART_2` TX | 9600 | 天猛星特殊引脚，使用前核对板载连接 |
| UART3_RXD | PA22 | `UART_2` RX | 9600 | 已配置 |

外接串口设备时需要 TX/RX 交叉并共地。PA10/PA11 与板载 USB 串口连接时，不要再让另一个外部设备同时驱动同一根 RX/TX 网络。

## 2. 灰度传感器当前配置

本工程使用的 8 路灰度模块不是 8 路并行输入，而是通过 `AD0/AD1/AD2` 写入三位通道地址，再从 `OUT` 读取被选中通道的数字量。

| 信号 | 当前 MCU 引脚 | 方向 | 当前状态 |
| --- | --- | --- | --- |
| AD0 | PA18 | GPIO 输出 | 已显式分配 |
| AD1 | PB15 | GPIO 输出 | 当前为 SysConfig 求解结果 |
| AD2 | PA14 | GPIO 输出 | 当前为 SysConfig 求解结果 |
| OUT | PB17 | GPIO 输入 | 当前为 SysConfig 求解结果 |

扩展板原图中的 `L4/L3/L2/L1/R1/R2/R3/R4` 八路并行接口与该模块不兼容。硬件应改成 `5V/GND/AD0/AD1/AD2/OUT` 四个 IO 信号，并在最终接线确定后把四个引脚写成明确的 `$assign`。

模块资料只明确标注 5 V 供电，尚未确认 OUT 输出电压以及 AD0/AD1/AD2 的高电平门限。接入 MCU 前必须实测或查明电平，不能让 OUT 向 MSPM0 输入 5 V；必要时增加双向电平转换或单独的输入分压。

## 3. 天猛星开发板特殊引脚

天猛星资料将 PA21、PA23、PA2、PA18、PA10、PA11 标为特殊引脚。当前工程有意使用了 PA21、PA2、PA18、PA10、PA11；制作或焊接扩展板前应再次核对开发板原理图、跳线和板载外设，避免同一网络被板载器件和扩展板同时驱动。

## 4. 当前待处理清单

1. 根据修改后的扩展板原理图锁定灰度模块 `AD0/AD1/AD2/OUT` 最终引脚。
2. 将 `MPU_INT/PA25` 改为 GPIO 输入，并决定是否启用边沿中断。
3. 为 `ECHO/PA7` 配置 TIMG7 CCP1 输入捕获或 GPIO 边沿计时，并确认 5 V 电平转换。
4. 统一两路舵机的 PWM1/PWM2 软件名称与扩展板丝印。
5. 实现编码器 `GROUP1_IRQHandler()`、正交查表解码、累计计数和周期速度计算。
6. 评估 `R_ENCA/PB22` 与板载 LED 的硬件冲突，必要时更换引脚。
7. 根据实际外设协议确认 `UART_1/UART_2`（扩展板 UART2/UART3）的波特率、收发模式和接线用途。

## 5. 修改和验证流程

修改引脚或外设后按以下顺序验证：

```powershell
cmake --build build --target syscfg
cmake --build build
```

生成完成后检查 `build/syscfg/ti_msp_dl_config.h` 中的实例名、GPIO 宏、IRQ 宏和定时器时钟。SysConfig/编译通过只表示配置和代码可生成、可链接；电平、波形、方向和实际模块行为仍需在硬件上验证。
